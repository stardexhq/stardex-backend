{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "types": ["node"],
    // Node runs the .ts sources directly, so imports keep their .ts extension.
    "allowImportingTsExtensions": true,
    "noEmit": true
  },
  "include": ["src"]
}
